import { AlertTriangle, Mail, Phone, RefreshCcw, Home } from "lucide-react";
import { motion } from "framer-motion";

export default function IssuePage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-[#000000] px-4 ">
      <p className="text-white">The site is facing some issues , try contacting the developer...</p>
    </div>
  );
}
